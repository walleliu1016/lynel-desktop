## 0.0.20

### 新功能

- **Stop hook 注入**：临时 `settings.json` 注入 `Stop` hook，Claude 会话停止时 hookserver 接收事件并分发，会话状态自动置为「空闲」。

### 修复

- **移除「自动允许工具调用」开关**：删除通用设置中的该开关及其放行逻辑、`auto_allow_bash` 配置字段。因全局 `permissions.defaultMode: bypassPermissions` 已兜底放行，该开关此前实际不生效。
