## 0.0.24

### 修复

- **/clear 会话迁移失效修复**：Claude `/clear` 后约 20s+ 才写入新 jsonl（SessionStart:clear + file-history-snapshot），Lynel 的轮询等待仅 6s 就超时放弃，导致 rebind 不执行、左侧会话列表与当前会话停留在旧 id。轮询窗口延长至 30s（与 `/resume` 对齐），`/clear` 后列表能正确刷新到新会话。
