## 0.0.25

### 修复

- **DeepSeek Harness 在 mac 打包应用找不到 dsh**：macOS 从 Finder 启动时 PATH 精简（不含 nvm 目录），直接 spawn 全局 dsh 会 ENOENT，而终端里 `which dsh` 可见。改为复用 login-shell 解析的完整 PATH（与 Claude 启动一致），nvm 安装的 dsh（`~/.nvm/versions/node/.../bin/dsh`）可被正确找到。
