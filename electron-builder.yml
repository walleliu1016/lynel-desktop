appId: com.akke.lynel-desktop
productName: Lynel Desktop
copyright: Copyright © 2026
directories:
  output: dist
  buildResources: build
files:
  - dist-electron/**/*
  - src/renderer/dist/**/*
  - build/windows/trayicon.ico
  - build/darwin/trayicon.png
  - build/linux/trayicon.png
extraMetadata:
  main: dist-electron/src/main/index.js
icon: build/appicon.png
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
win:
  target:
    - target: nsis
      arch: x64
  icon: build/windows/icon.ico
mac:
  target:
    - target: dmg
      arch: x64
  category: public.app-category.developer-tools
  # hardened runtime + entitlements：Apple 公证（notarization）的前提
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  # 暂禁用公证：签名凭据未注入（CI 构建不签名不公证），待 macos-signing 方案落地后再开启
  notarize: false
linux:
  target:
    - target: AppImage
      arch: x64
  category: Development
  icon: build/linux/icon.png
artifactName: lynel-desktop-${version}-${arch}.${ext}

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true

publish:
  - provider: github
    owner: walleliu1016
    repo: lynel-desktop
    releaseType: release
